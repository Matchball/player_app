<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lenovo.matchpointf">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppThemeWithNoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignInActivity" />
        <activity
            android:name=".Screen2"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".Screen3" />
        <activity android:name=".TournamentDataActivity" />
        <activity android:name=".OverviewActivity" />
        <activity android:name=".LeagueTableActivity" />
        <activity android:name=".PlayerPersonalActivity" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name=".CenterInfo"
            android:theme="@style/AppTheme"/>
        <activity android:name=".Payment" />
    </application>

</manifest>